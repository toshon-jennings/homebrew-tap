name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    name: Audit formulae
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tap
        uses: actions/checkout@v4

      - name: Normalize GoReleaser-generated formula
        run: |
          set -euo pipefail
          for f in Formula/*.rb; do
            if head -5 "$f" | grep -q 'generated by GoReleaser'; then
              sed -i '/^  version "/d' "$f"
            fi
          done

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@1f8e202ffddf94def7f42f6fa3a482e821489f9c

      - name: Tap repository
        run: |
          brew untap toshon-jennings/tap 2>/dev/null || true
          brew tap toshon-jennings/tap "$PWD"
          brew trust toshon-jennings/tap

      - name: Audit formulae (strict, online)
        run: brew audit --strict --online cleanmac github-overview

      - name: Clean up tap
        if: always()
        run: brew untap toshon-jennings/tap 2>/dev/null || true

  style:
    name: Style check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tap
        uses: actions/checkout@v4

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@1f8e202ffddf94def7f42f6fa3a482e821489f9c

      - name: Tap repository
        run: |
          brew untap toshon-jennings/tap 2>/dev/null || true
          brew tap toshon-jennings/tap "$PWD"
          brew trust toshon-jennings/tap

      - name: Check formula style and syntax
        run: brew style cleanmac github-overview

      - name: Clean up tap
        if: always()
        run: brew untap toshon-jennings/tap 2>/dev/null || true

  test:
    name: Test ${{ matrix.formula }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
        formula: [cleanmac, github-overview]
    steps:
      - name: Checkout tap
        uses: actions/checkout@v4

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@1f8e202ffddf94def7f42f6fa3a482e821489f9c

      - name: Tap repository
        run: |
          brew untap toshon-jennings/tap 2>/dev/null || true
          brew tap toshon-jennings/tap "$PWD"
          brew trust toshon-jennings/tap

      - name: Install and test ${{ matrix.formula }}
        run: |
          brew install "${{ matrix.formula }}"
          brew test "${{ matrix.formula }}"

      - name: Clean up tap
        if: always()
        run: brew untap toshon-jennings/tap 2>/dev/null || true
